program ejercicio5;
    type
        tNombres= (Maia, Lucas, Xanax, Ginebra, walter, euge, carlos); 
    var 
        nombre: string;
        var1:tNombres;
    procedure EscribirNombre(nombre: tnombres);   

    begin
        read(nombre);
        if (nombre=var1) then
        begin
        writeln(nombre);
        end
    end.
    